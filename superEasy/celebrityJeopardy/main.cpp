#include <bits/stdc++.h>
#define deb(x) cerr<<#x<<": "<<x<<'\n'
#define rep(i,n) for(int i=0;i<n;++i)
using namespace std;

int main(){
  string s;
  while(getline(cin, s)){
    printf("%s\n",s.c_str());
  }
  return 0;
}
